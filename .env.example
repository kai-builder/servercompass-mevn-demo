# Server Compass MEVN Demo — Environment Variables
# Copy this file to .env and fill in your values.

# ── Application ──────────────────────────────────────────────────────────────
APP_NAME=ServerCompass MEVN Demo
API_URL=https://api.servercompass.app
ENVIRONMENT=production
VERSION=1.0.0

# ── Database (MongoDB) ────────────────────────────────────────────────────────
# Private — never exposed to the Vue frontend
DATABASE_URL=mongodb://user:password@localhost:27017/servercompass

# ── Security ──────────────────────────────────────────────────────────────────
# Private — never exposed to the Vue frontend
API_SECRET_KEY=your-secret-key-here

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production
